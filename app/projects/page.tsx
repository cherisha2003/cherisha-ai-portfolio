import Link from "next/link"; import { ArrowRight } from "lucide-react"; import { PageShell } from "../components"; import { projects } from "../content";
export default function Projects(){return <PageShell><section className="page-intro clean-shell"><p>Selected work</p><h1>Projects built around<br/>real problems.</h1><span>Software engineering and data-analysis work, with the decisions, data, contribution, and outcomes behind each project.</span></section><section className="project-list clean-shell">{projects.map((p,i)=><Link href={`/projects/${p.slug}`} className="project-list-item" key={p.slug}><span>{String(i+1).padStart(2,"0")}</span><div><small>{p.type} · {p.year}</small><h2>{p.title}</h2><p>{p.summary}</p><div className="tags">{p.stack.slice(0,4).map(s=><b key={s}>{s}</b>)}</div></div><aside><em>{p.status}</em><ArrowRight/></aside></Link>)}</section></PageShell>}
