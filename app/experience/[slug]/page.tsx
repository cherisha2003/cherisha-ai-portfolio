import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowLeft } from "lucide-react"; import { PageShell } from "../../components"; import { experiences } from "../../content";
export function generateStaticParams(){return experiences.map(e=>({slug:e.slug}))}
export default async function ExperienceDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const e=experiences.find(x=>x.slug===slug);if(!e)notFound();return <PageShell><article className="detail clean-shell"><Link className="back" href="/experience"><ArrowLeft/> All experience</Link><header><div><p>{e.year} · {e.company}</p><h1>{e.role}</h1><span>{e.summary}</span></div><aside><small>Company</small><b>{e.company}</b><small>Focus</small><b>{e.tools.join(" · ")}</b></aside></header><section className="detail-grid"><h2>What I contributed</h2><ol>{e.details.map(x=><li key={x}>{x}</li>)}</ol></section><section className="detail-grid"><h2>Skills used</h2><div className="tags large">{e.tools.map(x=><b key={x}>{x}</b>)}</div></section></article></PageShell>}
